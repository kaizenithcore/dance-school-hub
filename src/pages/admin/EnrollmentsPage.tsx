import { PageContainer } from "@/components/layout/PageContainer";

export default function EnrollmentsPage() {
  return (
    <PageContainer title="Inscripciones" description="Revisa y gestiona las inscripciones">
      <div className="rounded-lg border border-border bg-card p-8 shadow-soft flex items-center justify-center min-h-[400px]">
        <p className="text-muted-foreground text-sm">La gestión de inscripciones se implementará en el Sprint 8</p>
      </div>
    </PageContainer>
  );
}
